/**
 * PostCSS Configuration
 * Uses the official @tailwindcss/postcss plugin to process
 * Tailwind CSS directives (imported via globals.css).
 */
const config = {
  plugins: {
    "@tailwindcss/postcss": {},
  },
};

export default config;
